<div class="container">
  <% @articles.each do |article| %>
    <div class="subcontainer">
      <h2><%= link_to article.title, article_path(article.id) %></h2>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
